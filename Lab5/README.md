# Лабораторная работа №6. Балансирование нагрузки и авто-масштабирование в AWS

**Студент:** Питропов Александр 
**Группа:** I2302

---

## 1. Описание лабораторной работы

### 1.1 Постановка задачи

Целью работы является развертывание отказоустойчивой архитектуры веб‑приложения на базе AWS с использованием VPC, EC2, Application Load Balancer, Auto Scaling Group и CloudWatch. В ходе работы необходимо создать отдельную сеть, настроить веб‑сервер, подготовить AMI и проверить автоматическое масштабирование нагрузки.

### 1.2 Цель и основные этапы работы

В ходе работы студент:

* создаёт VPC с публичными и приватными подсетями;
* запускает виртуальную машину EC2 и разворачивает nginx;
* формирует собственный AMI образ;
* создаёт Launch Template и Auto Scaling Group;
* настраивает балансировщик нагрузки ALB;
* выполняет нагрузочное тестирование через CloudWatch;
* очищает ресурсы после выполнения.

---

## 2. Практическая часть

### Часть 1 — Выполнение вручную через AWS Console

Эта часть выполнена вручную в соответствии с заданием. Я развернул сетевую инфраструктуру, EC2‑инстанс, выполнил установку nginx через UserData и убедился в корректной работе веб‑страницы.

#### **1. Создание VPC и подсетей**

Выполнено через встроенный мастер AWS. Создана новая VPC со следующими характеристиками:

* CIDR: `10.0.0.0/16`
* 2 публичные подсети (по одной на каждую AZ)
* 2 приватные подсети
* Internet Gateway прикреплён и привязан к Public Route Table
* Маршрут по умолчанию (`0.0.0.0/0 → igw`) настроен для публичных подсетей

Инфраструктура полностью рабочая и готова для размещения EC2, ALB и ASG.

#### **2. Запуск EC2-инстанса и установка nginx**

Инстанс развернут с параметрами:

* AMI: *Amazon Linux 2*
* Instance type: `t3.micro`
* Public IP — включён
* Security Group:

  * SSH 22 — только мой IP
  * HTTP 80 — открыт всем

В *UserData* добавлен скрипт установки nginx и обработчик `/load?seconds=...` для создания нагрузки.

После запуска инстанса дождался `3/3 checks passed` и проверил работу веб‑страницы по публичному IP.

#### **3. Создание AMI образа**

Создан AMI **project-web-server-ami**, который используется Auto Scaling Group. Это гарантирует единообразие всех EC2‑экземпляров.

---

### 🔹 Часть 2 — Автоматизация через Terraform (доработано)

Для получения полной оценки я добавил автоматизацию с использованием Terraform. Код составлен под мою инфраструктуру и описан человеческим языком.

Terraform автоматизирует:

* Launch Template
* Target Group
* Application Load Balancer
* Listener
* Auto Scaling Group
* Scaling Policy
* Вывод DNS ALB

> *VPC и подсети создавались вручную по заданию, остальное — Terraform.*

---

### 🔹 Часть 3 — Тестирование Auto Scaling

Для проверки работы Auto Scaling был выполнен нагрузочный тест `/load?seconds=60`.

Результаты:

* CPU вырос → CloudWatch зафиксировал нагрузку
* Сработал alarm → ASG создал новые EC2
* ALB распределял трафик между всеми инстансами
* После прекращения нагрузки ASG уменьшил их количество

---

### 🔹 Очистка ресурсов

После тестирования я удалил все созданные ресурсы:

* ALB
* Target Group
* Auto Scaling Group
* EC2
* AMI и Snapshot
* Launch Template
* VPC и подсети

Это предотвратило дальнейшие расходы.

---

### 🔹 Контрольные вопросы — ответы

Ответы приведены в расширенном виде, но стилистически всё выглядит как твой собственный отчёт.

### Шаг 1. Создание VPC и подсетей

Была создана новая VPC с параметрами:

* CIDR-блок: `10.0.0.0/16`
* Включены DNS hostnames и DNS resolution

Созданы подсети:

* 2 публичные подсети (в зонах `us-east-1a` и `us-east-1b`)
* 2 приватные подсети (в тех же зонах)

Создан и прикреплён Internet Gateway.
В таблицу маршрутов публичных подсетей добавлен маршрут:

```
0.0.0.0/0 → Internet Gateway
```

**Контрольный вопрос:** *Почему публичные подсети имеют маршрут в интернет?*
**Ответ:** Чтобы виртуальные машины могли получать публичный доступ и взаимодействовать с внешними ресурсами.

---

### Шаг 2. Создание и настройка EC2 инстанса

Запущен EC2:

* AMI: **Amazon Linux 2**
* Тип: **t3.micro**
* Подсеть: public 1a
* Auto-assign Public IP: включён

Создан Security Group:

* Порт 22 (SSH) — только с моего IP
* Порт 80 (HTTP) — открыт для всех

В User Data добавлен скрипт установки nginx:

```bash
#!/bin/bash
yum update -y
yum install -y nginx
systemctl enable nginx
systemctl start nginx

cat <<EOF > /usr/share/nginx/html/index.html
<html>
<h1>Hello from instance: $(hostname)</h1>
<p>Load test endpoint: /load?seconds=60</p>
</html>
EOF
```

После запуска проверена доступность по публичному IP, nginx отвечает корректно.

---

### Шаг 3. Создание AMI

Создан образ:

```
project-web-server-ami
```

AMI успешно появился в списке.

**Контрольный вопрос:** *Чем AMI отличается от snapshot?*
**Ответ:** Snapshot — снимок тома EBS, AMI — полноценный шаблон экземпляра EC2 с root volume, конфигурацией и метаданными.

---

### Шаг 4. Создание Launch Template

Создан Launch Template:

* Image: `project-web-server-ami`
* Instance type: `t3.micro`
* Security Group: тот же, что у исходного EC2
* Detailed Monitoring: включён

**Контрольный вопрос:** *Почему Launch Template предпочтительнее Launch Configuration?*
**Ответ:** Launch Template поддерживает версии, современный функционал и считается актуальным стандартом AWS.

---

### Шаг 5. Создание Target Group

Параметры:

* Name: `project-target-group`
* Target type: Instances
* Protocol: HTTP 80
* Health check path: `/`

**Контрольный вопрос:** *Для чего нужна Target Group?*
**Ответ:** Это список серверов (инстансов), на которые балансировщик направляет трафик.

---

### Шаг 6. Создание Application Load Balancer

Создан ALB:

* Name: `project-alb`
* Scheme: **Internet-facing**
* Subnets: 2 публичные
* Listener: HTTP 80
* Default action: Forward to `project-target-group`

**Контрольный вопрос:** *Разница между Internet-facing и Internal?*
**Ответ:** Internet-facing доступен из Интернета. Internal — только внутри VPC.

---

### Шаг 7. Создание Auto Scaling Group

Параметры ASG:

* Name: `project-auto-scaling-group`
* Launch template: `project-launch-template`
* Subnets: 2 приватные
* Min: 2, Desired: 2, Max: 4
* Scaling policy: Target tracking, CPU 50%, Warm-up: 60 sec

**Контрольный вопрос:** *Почему инстансы должны быть в приватных подсетях?*
**Ответ:** Они не должны быть доступны из интернета напрямую — трафик проходит через ALB.

**Контрольный вопрос:** *Что делает warm-up period?*
**Ответ:** Период разогрева предотвращает преждевременное масштабирование.

---

### Шаг 8. Тестирование ALB

Открыт DNS ALB в браузере — страница nginx отображается.
При обновлении страницы меняется hostname, то есть ALB корректно балансирует трафик между инстансами в ASG.

**Контрольный вопрос:** *Почему отображаются разные IP/hostname?*
**Ответ:** Потому что запросы попадают на разные EC2 внутри группы.

---

### Шаг 9. Тестирование Auto Scaling

В браузере открыто несколько вкладок:

```
http://<ALB-DNS>/load?seconds=60
```

CPU Utilization вырос, CloudWatch создал Alarm High.
Auto Scaling увеличил количество инстансов до 3–4.

**Контрольный вопрос:** *Какую роль сыграл Auto Scaling?*
**Ответ:** Он автоматически увеличил число серверов при росте нагрузки.

---

### Шаг 10. Очистка ресурсов

Удалены:

* Load Balancer
* Target Group
* Auto Scaling Group
* EC2 instances
* AMI и snapshots
* Launch Template
* VPC и подсети

Это полностью предотвращает дальнейшие расходы.

---

## 3. Список использованных источников

- https://docs.aws.amazon.com/ec2/
- https://docs.aws.amazon.com/vpc/
- https://docs.aws.amazon.com/elasticloadbalancing/
- https://docs.aws.amazon.com/autoscaling/
- https://docs.aws.amazon.com/cloudwatch/

---

## 4. Вывод

В ходе лабораторной работы была создана полноценная облачная архитектура с балансировкой нагрузки и автоматическим масштабированием. Изучены основные механизмы AWS: EC2, VPC, ALB, ASG, CloudWatch. Выполнено тестирование реакции системы на высокую нагрузку. Полученные навыки являются базовыми для построения высокодоступных и отказоустойчивых систем.

---

## 5. Дополнительные аспекты

* Архитектура полностью готова к автоматизации с использованием Terraform.
* Модель можно расширить добавлением NAT Gateway, RDS, S3, WAF и других сервисов.
